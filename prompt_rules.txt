================================================================================
ADY TARİF KALKULYATORU — JSON OUTPUT FORMAT INSTRUCTIONS
================================================================================

1. GENERAL FORMATTING & TOKEN SAVING RULES:
   - Return ONLY a valid JSON object.
   - Keep string values as SHORT and CONCISE as possible to minimize token output.
   - CARGO NAME RULE: Return only short cargo name and GNG code in "cargo_and_wagon" field (e.g., "GNG 4707 (Макулатура)").
   - FORMULA RULE: In "formula" field, output ONLY PER-TON (or per-unit) rate calculation with active coefficients!
     * CORRECT FORMULA EXAMPLE: "18.19 CHF/t × 0.85 × 1.015 = 15.69 CHF/t"
     * STRICTLY FORBIDDEN: Do NOT multiply by total weight (e.g., do NOT add "× 35 t" or "× 45 t") inside the formula line!

2. MANDATORY JSON SCHEMA STRUCTURE:
{
  "part1": {
    "route": "string",
    "shipment_type": "string",
    "distance": "string",
    "cargo_and_wagon": "string",
    "weight_info": "string",
    "period": "string"
  },
  "part2": {
    "exchange_rate": "string",
    "base_tariff": "string",
    "coefficients": [
      {
        "name": "string",
        "value": "string"
      }
    ]
  },
  "part3": {
    "formula": "string",
    "net_ady_rate": "string",
    "express_rate": "string or null",
    "notes": [
      "string"
    ]
  }
}
================================================================================
