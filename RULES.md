# RULES.md — Техническая спецификация и правила калькулятора ADY

## 1. Архитектурные принципы
* **NLU & Парсинг (Gemini API):** Отвечает исключительно за извлечение ключевых сущностей (raw_from, raw_to) из свободной речи пользователя. Модель НЕ выполняет тарифных расчетов и не определяет ЕСР-коды самостоятельно.
* **Детерминированный движок (Python Backend):** Выполняет 100% бизнес-логики, включая определение типов перевозки (Транзит, Импорт, Экспорт, Внутренняя), поиск расстояний и итоговое форматирование.
* **Централизация синонимов (data/stations_mapping.py):** Единственный источник правды для всех алиасов, опечаток, исторических названий, кодов ЕСР и пограничных статусов. Вся сложная логика синонимов инкапсулирована внутри справочника, модуль core/router.py очищен от условных операторов if/elif для поиска имён.

---

## 2. Принципы разработки и командного взаимодействия
* **Строгий режим кода:** Не угадывать, без заглушек, не ломать рабочий код, минимальные изменения, чёткие блоки для вставки.
* **Запрос информации:** Если не хватает информации — сразу спросить.
* **Контроль контекста:** Если теряется контекст или есть неуверенность в памяти — сразу предупредить пользователя.

---

## 3. Логика типов перевозок (ShipmentType)
Определение типа перевозки происходит автоматически на основе пограничных статусов (is_border) начальной и конечной станций:

1. **Транзит (Transit):** Откуда (is_border=True) -> Куда (is_border=True)
   Пример: Ялама -> Беюк Кясик, Курык -> Беюк Кясик, Астара -> ТРК.
2. **Импорт (Import):** Откуда (is_border=True) -> Куда (is_border=False)
   Пример: Ялама -> Абшерон, ТРК -> Баладжары.
3. **Экспорт (Export):** Откуда (is_border=False) -> Куда (is_border=True)
   Пример: Абшерон -> Ялама, Гянджа -> Беюк Кясик.
4. **Внутренняя (Local/Domestic):** Откуда (is_border=False) -> Куда (is_border=False)
   Пример: Хырдалан -> Гянджа, Абшерон -> Баладжары.

---

## 4. Особая бизнес-логика паромных переправ и узлов (Алят)
* **Привязка к ЕСР:** Паромные и морские направления привязаны к соответствующим экспортным терминалам Алята для точного поиска расстояний в матрице:
  * Курык / Курик / Актау -> ЕСР 553002 (Ələt eksport-Kurik)
  * ТРК / Туркменбаши / Туркмен -> ЕСР 548803 (Ələt eksport-Türk.)
  * Актау -> ЕСР 549204 (Ələt eksport-Aktau)
* **Правило форматирования вывода ("Алят-эксп."):**
  Если пользователь напрямую запрашивает сокращения вроде «Алят экс/експ», система корректно определяет узел, но при форматировании вывода в интерфейсе скрывает код ЕСР, отдавая чистый интерфейсный вид: Алят-эксп. (без скобок с кодом).

---

## 5. Требования к коду и оформлению
* **Язык комментариев:** Все примечания и комментарии в коде оформляются СТРОГО НА РУССКОМ ЯЗЫКЕ.
* **Визуальные блоки:** Разделы файлов размечаются стандартизированными визуальными рамками вида:
  # ------------------------------------------------------------------------------
  # БЛОК X: Название блока на русском языке
  # ------------------------------------------------------------------------------
* **Многоязычность (AZ / RU / EN):** Все станции должны иметь локализованные названия для трех языков вывода с сохранением единого азербайджанского канонического ключа.

---

## 6. Покрытие тестами (tests/test_routes.py)
Автоматические тесты проверяют 100% ключевых транспортных сценариев:
1. Валидацию маршрутов всех 4 типов перевозок.
2. Корректность распознавания пользовательских синонимов на русском, азербайджанском и английском языках.
3. Отсутствие вывода ЕСР-кода для специфических пользовательских запросов («Алят-эксп.»).

---

## 7. Логика расчета тарифов, Валютная система (USD / CHF) и Таблицы 3 и 4
1. **Валюта расчета:** Все итоговые тарифы провозной платы выводятся строго в **долларах США (USD)**.
2. **Валюта базовых ставок:** Все базовые таблицы (Таблица 3, Таблица 4) хранят тарифные ставки в **Швейцарских франках (CHF)** за 1 тонну.
3. **Конвертация валют:** Курс CHF -> USD подтягивается динамически из `data/currency_rates.py` по дате перевозки `shipment_date` (курс 0.79 для Q3 2026, 0.81 для Q4 2026).
   $$\text{Базовая ставка USD} = \frac{\text{Базовая ставка CHF}}{\text{Курс конвертации}}$$
4. **Выбор Базовой Таблицы:**
   * **Импорт / Экспорт:** Используют **Таблицу 3** (`data/Table_3_Tariffs.txt`, модуль `core/tables/table_3.py`) для универсальных вагонов.
   * **Транзит:** Использует **Таблицу 4** (`data/Table_4_Tariffs.txt`, модуль `core/tables/table_4.py`) для универсальных вагонов (крытый, полувагон, платформа).
   * **Специализированный подвижной состав (СПС):** Использует **Таблицу 5** (`data/Table_5_Tariffs.txt`, модуль `core/tables/table_5.py`) для рефрижераторных вагонов/секций, АРВ, ИВ-термосов,         ледников, автомобилевозов, двухъярусных платформ, ИНВ/АНВ и дизель-генераторов.
5. **Последовательность умножения коэффициентов:**
   * **Шаг 1:** Базовая ставка в USD ($\text{CHF} / \text{Курс}$).
   * **Шаг 2:** Специфические коэффициенты по грузу/маршруту (например, 1.04 для леса и черных металлов).
   * **Шаг 3:** Коэффициент международной груженой перевозки (1.015).
   * **Шаг 4:** Коэффициент приватного вагона СПС (0.85).

---

## 8. Расширенное тестовое покрытие (tests/test_full_calculation.py)
Автоматический тестовый набор `pytest` проверяет полный цикл тарифного расчета:
1. **Импорт пшеницы (Ялама -> Абшерон):** Таблица 3, минимальная норма 60т, вагон СПС (0.85), международный (1.015).
2. **Экспорт леса (Абшерон -> Ялама):** Таблица 3, спецкоэффициент на лес (1.04), вагон СПС (0.85).
3. **Транзит угля (Ялама -> Беюк Кясик):** Таблица 4, полувагон 60т, базовая ставка 28.53 CHF/т.
4. **Изделия из бумаги МПС (Баладжары -> Беюк Кясик):** Таблица 3, инвентарный вагон МПС (без коэф. 0.85).
5. **Экспорт металлов (Алят-эксп -> Беюк Кясик):** Расчет на ноябрь 2026 года (курс Q4 = 0.81), крытый вагон 35т, СПС.
6. **Импорт химии с парома (Курык -> Хырдалан):** Авто-определение парома Курык как `Ələt eksport-Kurik`, Импорт (Таблица 3), платформа 50т, СПС.
7. **Транзит зерна на Туркменбаши (Ялама -> ТРК):** Авто-определение ТРК как `Ələt eksport-Türk.`, Транзит (Таблица 4), хоппер 55т, СПС.
8. **Динамический вывод:** Автоматическое определение названия базовой таблицы `(Таблица 3)` или `(Таблица 4)` в тексте вывода.

---
## 9. Архитектура и правила разделения ответственности (Separation of Concerns)

** **Локальные правила таблиц — строго внутри своих модулей:**
  Все специфичные коэффициенты и условия, относящиеся к конкретной тарифной сетке, должны вычисляться исключительно внутри своего модуля (`core/tables/table_X.py`). 
  *Пример (Таблица 5):* Модуль `core/tables/table_5.py` обслуживает **специализированный подвижной состав (СПС)**. Внутри него строго изолированно рассчитываются:
    * Логика выбора колонок по весу (<25т / >=25т) и состоянию (гружёный/порожний).
    * Коэффициенты составности рефсекции: 1 вагон (1.70), 2 вагона (1.40), 3 вагона (1.10), 4 вагона (1.00), 5+ вагонов (0.85).
    * Скидка 0.60 на плодоовощную продукцию стран СНГ/ТС (коды ГНГ 07, 08, 12129100).
    * Аксиальные ставки: 0.12 CHF/ось-км (для дизель-генераторов) и 0.10 CHF/ось-км (для порожнего вагона в составе гружёной секции).
    * Коэффициент 0.80 для двухъярусных платформ-автовозов..

---

## 10. Разделение ответственности и обработка расстояний (Router vs Calculator)

* **Чистое разделение ролей (Router / Calculator):**
  * **Роутер (`core/router.py`):** Занимается географической и нормативной обработкой маршрута. Рассчитывает фактическое расстояние из CSV (`distance_km`) и проверяет тарифные минимумы ADY ($101\text{ км}$ для экспорта, $151\text{ км}$ для импорта). При срабатывании правила формирует нормативное значение `calculated_distance_km`, а также записывает метаданные правила (`rule_code` и `params`: `actual` и `applied`).
  * **Калькулятор (`core/calculator.py`):** Выступает в роли «чистого Повара». Принимает отфильтрованный нормативный километраж (`route_res.calculated_distance_km`) через параметр `distance_km` и выполняет строго финансовые расчёты базовых ставок и коэффициентов.
* **Сохранение метаданных для отчётов:**
  * Фактическое расстояние из базы данных ($67\text{ км}$) не теряется и сохраняется в объекте `RouteResult`.
  * При передаче данных в интерфейсы (`app.py`, тесты, печать) уведомление о применённом минимальном расстоянии формируется на основе `route_res.rule_code` и `route_res.params`, обеспечивая прозрачность расчётов для пользователя на всех поддерживаемых языках (RU / AZ / EN).

* **Централизация сквозных правил (`core/main_rules.py`):**
  Правила Тарифного руководства, применяемые к нескольким таблицам одновременно (например, коэффициент 1.20 на цветные/драгоценные металлы по п. 3.1.1 для Таблиц 3 и 4, надбавка 1.50 на импорт/экспорт, индексация 1.015, скидка 0.85 на приватные вагоны), должны жить строго в `core/main_rules.py`. Дублировать сквозную логику в модулях таблиц запрещено.

* **Роль главного калькулятора (`core/calculator.py`):**
  Файл `core/calculator.py` выступает исключительно в роли чистого оркестратора. В нём запрещен хардкод коэффициентов и ручная проверка условий правил. Его задача — вызвать модуль соответствующей таблицы, запустить `apply_main_rules`, рассчитать итоговые суммы и собрать единый список уведомлений.

* **Прозрачность вычислений и структура ответа:**
  Каждый модуль расчета (`main_rules` и модули из `tables/`) должен возвращать не просто числовое значение ставки (`float`), а словарь с детальной структурой: итоговую/базовую ставку и список примененных правил `applied_rules` с указанием `rule_code` и `params`.

* **Строгая локализация без заглушек:**
  Все `rule_code` должны строго соответствовать ключам из `data/translations.py`. Запрещено внедрять ручной текстовый вывод или временные заглушки прямо в код расчетов или тестов.

  ## 11. 3.1.2.7. Перевозка на специализированных платформах (>19 м)
- **Условия применения**:
  - Тип вагона: специализированная платформа для крупнотоннажных контейнеров (`is_specialized_platform = True`).
  - Длина: расстояние между осями сцепа автосцепок более 19 метров (`coupling_distance_over_19m = True`).
  - Принадлежность: инвентарный парк / МПС (`is_private_wagon = False`).
  - Груз: габаритный / очерченный (`is_oversized_cargo = True`).
- **Расчёт тарифа**:
  - Базовая ставка определяется по **Таблице 3** (Импорт/Экспорт/Внутренняя) или **Таблице 4** (Транзит).
  - К итоговой ставке применяется повышающий коэффициент **1.20** (`MAIN_COEFF_SPECIAL_PLATFORM_OVER_19M_1_20`).

### 3.1.2.8. Перевозка в других видах специализированного подвижного состава
- **Условия применения**:
  - Грузы, перевозимые в специализированных вагонах, не подпадающие под пункты 3.1.2.1–3.1.2.7.
- **Расчёт тарифа**:
  - Провозная плата рассчитывается по базовым тарифным ставкам для универсальных вагонов (**Таблицы 3 и 4**) без дополнительных специальных надбавок.
 
 ## 11. ### 3.2.2. Перевозка собственных (приватных) и арендованных порожних вагонов

- **Коды ГНГ (YHN)**: `99210000`, `99213000`, `99220000`, `99223000`.
- **Условия применения**:
  - Вагон следует в порожнем состоянии (`is_empty_wagon = True`).
  - Принадлежность: собственный / приватный / арендованный (`is_private_wagon = True`).
  - Тип вагона: любой, кроме транспортеров (`wagon_type != 'transporter'`).
- **Расчёт тарифа**:
  - Базовая ставка рассчитывается напрямую по формуле:
    $$\text{Ставка (CHF)} = \text{Расстояние (км)} \times \text{Количество осей} \times 0.10\text{ CHF}$$
  - Если количество осей не указано пользователем, по умолчанию принимается `axle_count = 4`.
  - Код ГНГ по умолчанию при отсутствии явного указания: `99220000`.
  - Стандартные **Таблицы 3 и 4** в данном расчете не используются.
  - Понижающий коэффициент приватного вагона `0.85` и коэффициент `1.015` **не применяются**.
  - Применяется код уведомления `MAIN_EMPTY_PRIVATE_WAGON_0_10_AXLE_KM`.
 
 ## 12. 3.2.6. Перевозка автопоездов, прицепов, полуприцепов и съемных автокузовов на спецплатформах

- **Типы подвижного состава**: `road_train`, `semi_trailer`, `road_train_platform`, `автопоезд`, `полуприцеп`.
- **Условия применения**:
  - Перевозка автопоездов, прицепов, полуприцепов и съемных автомобильных кузовов (кроме контейнеров) на собственных (приватных) или арендованных специализированных платформах.
  - Применяется как для груженого, так и для порожнего состояния.
- **Расчёт тарифа**:
  - Базовая ставка определяется strictly по **Таблице 5**:
    - **Гружёный состояние**: Используется **колонка 7** Таблицы 5 (за 1 тонну).
    - **Порожнее состояние**: Используется **колонка 8** Таблицы 5 (за вагон).
  - На порожнее состояние автотехники по п. 3.2.6 **не распространяется** расчет по Пункту 3.2.2 ($0.10\text{ CHF/ось-км}$).
  - Для собственных (приватных) платформ сработает понижающий коэффициент **0.85** (`MAIN_COEFF_0_85_PRIVATE_WAGON`).
  - Формируется код уведомления `ROAD_TRAIN_SPECIAL_PLATFORM_RULE_3_2_6`.
 
  ### Terminologiya / Терминология
- **İNV (İntermodal nəqliyyat vasitəsi)** — контейнеры, съемные автомобильные кузова и полуприцепы, перевозимые на двух и более транспортных средствах без выгрузки самого груза при смене вида транспорта.
- **ANV (Avtotransport nəqliyyat vasitəsi)** — груженый или порожний автомобиль, автопоезд и прицепы, находящиеся в движении до и после железнодорожного транспорта.

---

## 13. 3.3.1. Перевозка гружёных İNV и ANV на специализированных платформах
- **Типы подвижного состава**: `inv`, `anv`, `inv_anv`.
- **Условия применения**:
  - Перевозка гружёных интермодальных (İNV) и автотранспортных средств (ANV) на специализированных платформах.
- **Расчёт тарифа**:
  - Расчёт выполняется по **Таблице 5 (колонка 7)** за 1 тонну.
  - Минимальная расчётная масса составляет **10 тонн** вне зависимости от фактического веса и степени негабаритности.
  - При фактическом весе менее 10 тонн расчётная масса автоматически повышается до **10 тонн**, и генерируется код уведомления `INV_ANV_MIN_WEIGHT_10T_RULE_3_3_1`.
  - Вес İNV/ANV **не округляется** по градациям Таблицы 1.

---

### 3.3.2. Перевозка порожних автопоездов, прицепов, полуприцепов и съемных кузовов на спецплатформах
- **Типы подвижного состава**: 
  - Автопоезда/прицепы/полуприцепы: `road_train`, `semi_trailer`, `road_train_platform`, `автопоезд`, `полуприцеп`.
  - Съемные кузова: `auto_body`, `detachable_body`, `кузов`.
- **Условия применения**:
  - Порожний рейс указанных средств на специализированных платформах.
- **Расчёт тарифа**:
  - Расчёт выполняется по **Таблице 5 (колонка 8)** за вагон.
  - Фиксированная расчётная масса (независимо от фактического веса и степени негабаритности):
    - Для **автопоездов, прицепов и полуприцепов** — **7 тонн** (`EMPTY_ROAD_TRAIN_WEIGHT_7T_RULE_3_3_2`).
    - Для **съемных автомобильных кузовов** — **5 тонн** (`EMPTY_AUTO_BODY_WEIGHT_5T_RULE_3_3_2`).
  - Сохраняется генерация базового кода `ROAD_TRAIN_SPECIAL_PLATFORM_RULE_3_2_6`.
 
  ## 14. Контейнерные перевозки, специализированные контейнеры и дизель-генераторы (пп. 3.4.3 – 3.4.8)

### 14.1. Разделение единиц измерения (Штуки vs Тонны)
* **Повагонные отправки:** Тариф рассчитывается за 1 тонну расчетного веса и умножается на billable_weight (за исключением порожних вагонов по п. 3.2.2).
* **Контейнерные отправки (Таблицы 8 и 10):** Тариф рассчитывается **за 1 контейнер (штуку)**. В `core/calculator.py` расчетная стоимость за единицу НЕ умножается на фактический/расчетный вес груза.

---

### 14.2. Специализированные контейнеры по Таблице 10 (п. 3.4.3.1)
* **Категории:** Танк-контейнеры (`tank_container`), рефрижераторные контейнеры (`reefer_container`) и контейнеры для вина/соков (`wine_juice_container`).
* **Тарифная сетка:** Рассчитывается напрямую по **Таблице 10** (`data/Table_10_Tariffs.txt`) в зависимости от длины (20 или 40 футов) и состояния (гружёный / порожний).
* **Спецусловие для вин и соков (ГНГ 2009, 2204, 2205, 2206):** При перевозке в танк-контейнерах расчет выполняется по **Таблице 8** с коэффициентом **1.30**.

---

### 14.3. Вагоны-дизель-генераторы и проводники (п. 3.4.3.2)
* **Повагонная базовая ставка:** Рассчитывается по фиксированной осевой ставке:
  $$\text{Базовая ставка (CHF)} = \text{Расстояние (км)} \times \text{Количество осей} \times 0.12\text{ CHF}$$
* **Отмена коэффициента 0.85:** Понижающий коэффициент приватности `0.85` **не применяется** к вагону-дизель-генератору, так как ставка $0.12\text{ CHF/ось-км}$ уже фиксирована для собственных/арендованных вагонов.
* **Плата за проезд проводников:** Начисляется дополнительно в размере **$12.0\text{ CHF}$ за каждые начатые $100\text{ км}$ на каждого человека**:
  $$\text{Сбор за проводников (CHF)} = \lceil\text{Расстояние} / 100\rceil \times 12.0 \times \text{attendants\_count}$$
  Сбор переводится в USD по текущему курсу конвертации и суммируется к итоговому тарифу за вагон (`attendants_fee_usd`).
* **Сервисная бригада (`is_service_crew = True`):** Плата за проезд не взимается ($0\text{ CHF}$), формируется уведомление `SERVICE_CREW_FREE_RULE_3_4_3_2`.

---

### 14.4. Специализированные контейнеры по Таблице 8 (пп. 3.4.5 – 3.4.8)
Базовая ставка выбирается из **Таблицы 8** (`data/Table_8_Tariffs.txt`) по длине контейнера (10, 20, 30, 40, 45 футов). Исходная базовая ставка CHF не перемножается внутри модуля таблицы, а передается в виде чистой табличной ставки с добавлением соответствующего коэффициента в список `applied_rules`:

1. **Дизель-генераторные контейнеры (п. 3.4.5):**
   * Тип: `diesel_generator_container` (группа SC, ISO коды S4, SJ).
   * Коэффициент: **1.35** (`DIESEL_GENERATOR_CONTAINER_COEFF_1_35`).
2. **Контейнеры-платформы / Flatrack (п. 3.4.6):**
   * Тип: `container_platform` (группы PL, PF, PC, PS, PT, ISO коды PO–P9, PA, PB, PD и др.).
   * Коэффициент: **1.40** (`CONTAINER_PLATFORM_COEFF_1_40`).
3. **Открытые контейнеры / Open Top (п. 3.4.7):**
   * Тип: `open_top_container` (группа UT, ISO коды U9, UY).
   * Коэффициент: **1.40** (`OPEN_TOP_CONTAINER_COEFF_1_40`).
4. **Контейнеры специального назначения (п. 3.4.8):**
   * Тип: `special_purpose_container` (группы SN, SC, ISO коды S3, S6–S9, SG, SV и др.).
   * Коэффициент: **1.40** (`SPECIAL_PURPOSE_CONTAINER_COEFF_1_40`).
  
## 15. Раздел 3.5 — Негабаритные, длинномерные грузы, транспортеры и вагоны прикрытия (пп. 3.5.1 – 3.5.3)

### 15.1. Малая степень негабаритности (п. 3.5.1.1)
* **Условия:** Степени негабаритности 1-2 нижняя, 1-3 боковая, 1-2 верхняя (`small`, `1-2_bottom`, `1-3_side`, `1-2_top`).
* **Расчёт:** Выполняется по универсальным вагонам (**Таблицы 3 и 4**).
* **Минимальный вес:** Если фактический/нормативный вес менее **25 тонн**, расчётная масса принимается за **25 тонн** (`OVERSIZED_SMALL_DEGREE_RULE_3_5_1_1`).

---

### 15.2. Высокие степени негабаритности и Таблица 11 (п. 3.5.1.2)
* **3-я верхняя степень (`3_top`):**
  * Используется **Таблица 11** (колонки 1–5 в зависимости от массы).
  * К итоговой ставке применяется повышающий коэффициент **1.50** (`OVERSIZED_TABLE_11_TOP_3_RULE_3_5_1_2`).
  * Минимальная масса — **10 тонн** (при весе < 10т ставка берется за вагон по колонке 1).
* **3-5 нижняя, 4-5 боковая степени (`3-5_bottom`, `4-5_side`, `3-5_bottom_side`):**
  * Используется **Таблица 11** (колонки 6–10 в зависимости от массы).
  * К итоговой ставке применяется повышающий коэффициент **2.00** (`OVERSIZED_TABLE_11_HIGH_DEGREE_RULE_3_5_1_2`).
  * Минимальная масса — **10 тонн** (при весе < 10т ставка берется за вагон по колонке 6).

---

### 15.3. Перевозка на транспортерах (п. 3.5.1.3 и п. 3.5.1.5)
* **Минимальная нагрузка на ось:** Расчётная масса транспортера не может быть меньше **5 тонн на каждую ось** (4-осный $\ge$ 20т, 6-осный $\ge$ 30т, 8-осный $\ge$ 40т) (`TRANSPORTER_AXLE_WEIGHT_RULE_3_5_1_3`).
* **6-я степень негабаритности или транспортеры 12+ осей:** Применяется повышающий коэффициент **3.00** (`TRANSPORTER_SPECIAL_DEGREE_6_OR_12_AXLE_RULE_3_5_1_5`).

---

### 15.4. Вагоны прикрытия и защитные рамки (п. 3.5.3)
* **Условия:** Порожние контрольные вагоны прикрытия или вагоны с защитными рамками (`cover_wagons_count > 0`).
* **Расчёт ставки:** Начисляется дополнительный сбор за каждый 4-осный вагон за ось-километр:
  $$\text{Сбор (CHF)} = \text{Расстояние (км)} \times (\text{Количество вагонов} \times 4) \times \text{Ставка CHF}$$
  * Приватный вагон прикрытия: **0.30 CHF/ось-км**.
  * Инвентарный вагон прикрытия (МПС): **0.35 CHF/ось-км**.
* **Код уведомления:** `EMPTY_COVER_WAGON_AXLE_KM_RULE_3_5_3`.
* Сбор переводится в USD по курсу конвертации и суммируется к общей стоимости отправки.

## 16. Раздел 3.6 — Перевозка опасных грузов (пп. 3.6.1 – 3.6.3)

### 16.1. Повышающий коэффициент и базовая тарифная сетка (п. 3.6.1)
* **Применимость:** Опасные грузы классов 1, 5.2 (ООН 3101–3110), 6.2, 7, грузы из Таблицы 13, а также отправки в крытых вагонах, цистернах, бункерных полувагонах и контейнерах с сопровождением проводников.
* **Повагонные отправки в универсальных вагонах:** Расчёт выполняется по **Таблице 12** (тарифные ставки Таблицы 7, увеличенные в 2 раза). Выбор колонки определяется массой: `5t`, `10t`, `15t`, `20t`, `20_60t`.
* **Перевозка в цистернах и бункерных полувагонах:** Базовые ставки **Таблицы 6** умножаются на коэффициент **2.00** (за исключением метанола).
* **Контейнерные отправки (кроме танк-контейнеров):** Базовые ставки **Таблиц 7 и 9** умножаются на коэффициент **2.00**.
* **Танк-контейнеры (контейнеры-цистерны):** Базовые ставки **Таблицы 10** умножаются на коэффициент **2.00**.
* **Код уведомления:** `DANGEROUS_CARGO_COEFF_2_00_RULE_3_6_1`.

---

### 16.2. Собственные (приватные) и арендованные вагоны
* К рассчитанным тарифам для приватного или арендованного подвижного состава применяется понижающий коэффициент **0.85** (`DANGEROUS_CARGO_PRIVATE_WAGON_0_85_RULE_3_6_1`).

---

### 16.3. Вагоны прикрытия для опасных грузов (п. 3.6.3)
* За каждый порожний вагон прикрытия, указанный в накладной, начисляется сбор за ось-километр:
  * Инвентарный вагон (МПС / ADY): **0.35 CHF/ось-км**.
  * Собственный (приватный) или арендованный вагон: **0.30 CHF/ось-км**.
* **Код уведомления:** `DANGEROUS_CARGO_COVER_WAGON_RULE_3_6_3`.

## 17. Справочник опасных грузов по Таблице 13 (коды ООН / BMT №)

### 17.1. Автоматическая классификация опасных грузов
* **Условие:** При передаче 4-значного кода ООН (`un_code` / BMT №) система автоматически проверяет его по реестру `data/Table_13_Tariffs.txt`.
* **Применимость по типам вагонов (`Tətbiq_tipi`):**
  * `hamisi`: Повышающий коэффициент **2.00** применяется для любых видов подвижного состава.
  * `cen`: Повышающий коэффициент **2.00** применяется только для наливных отправок (цистерны, бункерные полувагоны, танк-контейнеры).
  * `ortulu_konteyner`: Повышающий коэффициент **2.00** применяется только для сухих видов подвижного состава (крытые вагоны, полувагоны, платформы, универсальные контейнеры).
* **Авто-переключение:** При совпадении условий автоматически активируется `is_dangerous_cargo = True` и включается расчет по **Таблице 12** (для универсальных вагонов) либо удвоение ставок Таблиц 6/10.
* **Код правила:** `DANGEROUS_CARGO_COEFF_2_00_RULE_3_6_1`.

## 18. Специфические условия перевозок и сопровождение (пп. 3.7 – 3.13)

### 18.1. Сцепы вагонов и негабаритное/длинномерное оборудование (п. 3.7)
* **Сцеп из 2+ вагонов (п. 3.7.1):** При перевозке на сцепе из двух и более вагонов провозная плата рассчитывается суммарно по фактической массе груза, но не менее **20 тонн на каждый вагон сцепа** (`ATTACHED_WAGONS_MIN_WEIGHT_RULE_3_7_1`).
* **Вагон в составе пассажирского поезда (п. 3.7.3):** К провозной плате за вагон применяется повышающий коэффициент **2.00** (`ROLLING_STOCK_PASSENGER_TRAIN_COEFF_2_00_RULE_3_7_3`).
* **Масса съёмного оборудования/приспособлений (п. 3.7.5):** Масса приспособлений прибавляется к массе груза (`attached_parts_weight`), уведомление `ATTACHED_PARTS_WEIGHT_ADDED_RULE_3_7_5`.

### 18.2. Перевозка проводников и отдельного вагона для проводников (п. 3.9)
* **Проезд проводников в грузовом вагоне (п. 3.9.1):** Начисляется сбор **$12.0\text{ CHF}$ за каждые начатые $100\text{ км}$ на человека** (`ATTENDANTS_FEE_RULE_3_9_1`).
* **Отдельный вагон для проводников / теплушка (п. 3.9.2–3.9.3):**
  * Грузовой вагон (крытый/платформа): **0.20 CHF/ось-км** (приватный) или **0.23 CHF/ось-км** (инвентарный).
  * Пассажирский вагон: **0.30 CHF/ось-км** (приватный) или **0.35 CHF/ось-км** (инвентарный).
  * Код уведомления: `SEPARATE_ATTENDANT_WAGON_RULE_3_9`.

### 18.3. Оборудование и несъёмные приспособления вагонов (п. 3.10)
* **Учет массы оборудования (п. 3.10.1):** Масса съёмного/несъёмного оборудования, не входящая в тару вагона (`equipment_weight`), прибавляется к массе груза. Код `EQUIPMENT_WEIGHT_ADDED_RULE_3_10_1`.
* **Порожний пробег спецвагонов с несъёмным оборудованием (п. 3.10.4):** Рассчитывается по фиксированной ставке **0.12 CHF / ось-км**. Код `NON_REMOVABLE_EQUIPMENT_WAGON_RULE_3_10_4`.

### 18.4. Перевозка усопших в гробах (п. 3.11)
* Перевозка гробов с телами усопших рассчитывается по общим правилам МТТ с применением понижающего коэффициента **0.10** (`COFFIN_TRANSPORT_COEFF_0_10_RULE_3_11`).

### 18.5. Отдельный локомотив (п. 3.12)
* При перевозке с использованием отдельного локомотива по заявке грузоотправителя применяется повышающий коэффициент **не менее 5.00** (`separate_locomotive_coeff >= 5.00`), код `SEPARATE_LOCOMOTIVE_COEFF_5_00_RULE_3_12`.

### 18.6. Сокращенный срок доставки (п. 3.13)
При согласовании ускоренной доставки к провозной плате применяются коэффициенты:
* В составе **грузового поезда**: коэффициент **1.50** (`EXPEDITED_DELIVERY_FREIGHT_TRAIN_COEFF_1_50_RULE_3_13`).
* В составе **пассажирского поезда**: коэффициент **2.00** (`EXPEDITED_DELIVERY_PASSENGER_TRAIN_COEFF_2_00_RULE_3_13`).
* В составе **контейнерного поезда**: коэффициент **1.00** (`EXPEDITED_DELIVERY_CONTAINER_TRAIN_COEFF_1_00_RULE_3_13`).

---

## 19. Домашняя утварь и правила перегрузки вагонов (Разделы 4 и 5)

### 19.1. Раздел 4 — Перевозка домашней утвари (ГНГ 9901)
* Грузы физических лиц под кодом ГНГ `9901` (`99010000`).
* В транзитном и международном сообщении рассчитываются по общим тарифным правилам Раздела 3 ADY, код уведомления `HOUSEHOLD_GOODS_RULE_4`.

### 19.2. Раздел 5 — Перегрузка из одного вагона в другие (пп. 5.1 – 5.2)
* **При изменении ширины колеи (1520 мм $\leftrightarrow$ 1435 мм) и на путях одинаковой колеи (пп. 5.1.2 / 5.2.1):**
  При перегрузке из 1 вагона в несколько на пограничной/перегрузочной станции каждая вновь сформированная вагонная отправка рассчитывается как **самостоятельная вагонная отправка** по её фактическому весу (`is_reloaded_part_shipment = True`), код `CARGO_RELOADED_SPLIT_WAGONS_RULE_5_1_2`.
* **Перегрузка остатков (п. 5.1.4):** Ставка за тонну определяется по суммарному весу всех частей.
* **Смена типа парка вагона (п. 5.2.2):** При перегрузке из приватного вагона в инвентарный плечи до и после станции перегрузки рассчитываются отдельно по соответствующим правилам для каждого типа вагона.

---

## 20. Оформление документов и порядок исчисления плат (Раздел 7)

### 20.1. Повагонный и поконтейнерный расчёт (п. 7.3)
* Если по одной накладной СМГС/ЦИМ перевозится несколько вагонов (кроме сцепов) или контейнеров, провозная плата рассчитывается **отдельно для каждой единицы** (вагона, контейнера или единицы на собственных осях). Код уведомления `SEPARATE_UNIT_CALCULATION_RULE_7_3`.

### 20.2. Изменение договора перевозки в транзите / Переадресовка (п. 7.4)
* При переадресовке груза в транзите расчёт провозной платы разбивается на два самостоятельных участка:
  1. От станции отправления до станции переадресовки.
  2. От станции переадресовки до новой станции назначения.
* Код уведомления: `REROUTING_SPLIT_CALCULATION_RULE_7_4`.
